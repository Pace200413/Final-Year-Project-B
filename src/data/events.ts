// src/data/events.ts
import type { CampusEvent } from "@/lib/types";

// Keep as empty if you don't want static events.
// Your UI will then show no events until you switch it to fetch from API.
export const EVENTS: CampusEvent[] = [];